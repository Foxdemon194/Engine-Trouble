using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeleteMe : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    //testing script, please delete
    // Update is called once per frame
    void Update()
    {
        
    }
}
